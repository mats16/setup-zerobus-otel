{
  "name": "setup-agent-otel",
  "version": "0.3.1",
  "description": "Interactive CLI to configure OpenTelemetry sending from Claude Code or Codex to Databricks Zerobus Ingest or any OTLP/HTTP backend",
  "type": "module",
  "bin": {
    "setup-agent-otel": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "format": "biome format --write .",
    "lint": "biome lint .",
    "check": "biome check --write ."
  },
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@inquirer/prompts": "^7.0.0",
    "smol-toml": "^1.4.2"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.12",
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.5.0"
  }
}
